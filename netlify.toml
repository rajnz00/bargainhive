[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "functions"
  included_files = ["data/products.json"]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
